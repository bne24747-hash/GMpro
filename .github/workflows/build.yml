name: Build ESP32 GMpro

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Update ke v4

      - name: Set up Python
        uses: actions/setup-python@v5 # Update ke v5
        with:
          python-version: '3.9'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Run Build
        run: pio run

      - name: Upload Binary
        uses: actions/upload-artifact@v4 # INI YANG PENTING: Update ke v4
        with:
          name: GMpro-Firmware
          path: .pio/build/esp32dev/firmware.bin
